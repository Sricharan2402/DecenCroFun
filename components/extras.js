import { createTheme } from "@mui/material/styles";

const black = createTheme({
    palette: {
        primary: {
            main: "#000000",
        },
        secondary: {
            main: "#ffffff",
        },
    },
});

export { black };
